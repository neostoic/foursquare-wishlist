<div class="header-menu-padding">
  <div class="text-right">
    <div class="text-user-name">
      <%= current_user.name %>
      <%= image_tag(current_user.photo) %>
    </div>
  </div>
</div>
<p>Wishlist:</p>
<div id="wishlist-container">
    <%= render partial: 'wishlist' %>
</div>
<p class="wishlist-padding">Checkins:</p>
<div id="list-container">
  <%= render partial: 'list' %>
</div>